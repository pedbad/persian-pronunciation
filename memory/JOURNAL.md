# JOURNAL — Session Log (append-only)

**What this is:** the project's memory across machines and Claude sessions. Every working session ends with a dated entry here: what was done, which checks passed or failed, open threads, and the exact next step. Every session *starts* by reading the latest entry.

**Sync rule:** once the GitHub repo exists (BUILD_PLAN Step 12), this folder lives in the repo — **pull at session start, commit + push this file at session end**. Until then it exists only on this machine.

**Public-repo rule:** the repo is public. Nothing sensitive goes in here — no passwords, no tokens, no student names or data. Secrets live in `.env` (git-ignored); people-data never enters the repo at all.

**Entry format:**

```
## YYYY-MM-DD — machine — short title
- Done: …
- Checks: passed/failed (which)
- Decisions/observations: … (real decisions go in DECISIONS.md; note the pointer here)
- Next step: BUILD_PLAN Step N
```

---

## 2026-08-07 — Pedram's Mac (Cowork session) — docs review, D9, corrections

- Done: Full review of all planning docs. Correction edits applied (boundary-tolerance rule ±50 ms *and* ≤30% of vowel duration; Whisper-gate false-rejection test; MFA `align_one`/latency notes; GOP optional column in Step 10; new Step 3b ethics/DPIA email; timeline revised). **D9 agreed and written**: two native references per lesson (`NativeReference` table), score against both, keep the better result, playback uses the winning voice, never score a clip against itself. All docs synced (DECISIONS, FABLE_REVIEW, BUILD_PLAN, HANDOVER, prototype banners). Draft emails written in `emails/` (tutor + DPIA/ethics) — **not yet sent**.
- Checks: n/a (documentation phase — zero code exists).
- Decisions: D3 amendment + D9, both dated 7 Aug 2026 in DECISIONS.md.
- Open threads: send the two emails; confirm the September developer (FABLE open question 2); second native speaker still unidentified (open question 1 — now load-bearing per D9).
- Next step: **BUILD_PLAN Step 1** (Part A system checks), in Claude Code on the Mac that will host the build.

## 2026-08-07 — MacBook "neo" (Claude Code) — Parts A and B complete; repo live

- Done: **BUILD_PLAN Steps 1–13, all green.** Part A (developer tooling): verified git 2.50.1, Homebrew 6.0.15, node 24.19.0 / npm 11.17.0 already present; installed Python 3.13.15, Docker Desktop 29.6.2 (Compose 5.3.1), ffmpeg/ffprobe 8.1.2, pre-commit 4.6.1, Praat 7.0. Part B: git identity already set (name kept as "Pedram Badakhchani", fuller than the plan's "Pedram"), `init.defaultBranch=main` added; the existing `id_ed25519` key was reused rather than generating a second one — GitHub accepted it first try. Cloned `langcen_base` to `persian-pronunciation`, renamed its remote to `scaffold`, created the public repo, pushed. Step 13 moved the planning docs into `docs/`, `memory/` to the repo root, `README.md` to the root (with its internal paths corrected for the new layout), and duplicated `HANDOVER.md` as `CLAUDE.md`. Folder-swap variant done: the local project root is now `~/Sites/persian`.
- Checks: every ✅ Check passed. The `hello-world` container ran; `ssh -T git@github.com` returned "Hi pedbad!"; `git push -u origin main` set upstream tracking; post-swap `diff -rq` confirmed every planning doc, `prototype/`, and `memory/` copied byte-identical.
- Failures and fixes: the Docker Desktop cask install failed when run non-interactively — it needs an administrator password to create `/usr/local/bin` — so Pedram ran it by hand. Two git tidy-ups also had to be run by hand: a Claude "GateGuard" hook blocks `git commit --amend` and `rm -rf` from the assistant. Mid-session the assistant's file-edit tool began failing with "Not logged in"; shell writes still worked, so the remaining edits went through the shell.
- Decisions/observations: no new DECISIONS.md entries. Two project conventions adopted: **no `Co-Authored-By` trailers** in commit messages (this is a public research repo), and `.claude/` plus `emails/` are git-ignored. New file `memory/SETUP_LOG.md` — a plain-English explanation of every installed tool and why this project needs it, intended both as methods-appendix material for the paper and as the reference for setting up a second machine; a `process` note recording it was added to RESEARCH_LOG.md. BUILD_PLAN's folder-swap note was amended: it previously told you to delete the backup folder without warning that `emails/` exists nowhere else. The backup is archived as `~/Sites/persian-private/archive/docs-preswap-2026-08-07.zip` — a frozen zip, deliberately not a second live copy of the planning documents — with `emails/` alongside it at `~/Sites/persian-private/emails/`.
- Flagged for later: `Dockerfile.engine` (Step 21) pins no versions — `pip install openai-whisper librosa …` installs whatever is newest on the day, which quietly undercuts D1's promise of a frozen environment. Propose pinned versions, probably an `engine/requirements.txt`, when Step 21 arrives; that is a change to the plan, so it needs a dated DECISIONS.md entry first.
- Open threads (unchanged): send the two draft emails; confirm the September developer; the second native speaker is still unidentified (load-bearing per D9). Repo Topics not yet set on GitHub.
- Next step: **BUILD_PLAN Step 14** (Part C) — venv, `pip install -r requirements.txt -r requirements-dev.txt`, `npm install`, `cp .env.example .env` and set the dev email path, `python check_env.py`, `migrate`, `npm run dev`. Note that `.env`'s `EMAIL_FILE_PATH` must read `/Users/ped/Sites/persian/tmp_emails` after the folder swap.

## 2026-08-07 — MacBook "neo" (Claude Code) — native-reference recording corpus expanded

- Done: **Open question 1 (the second native speaker) now has a plan, not just a hope.** Recordings will be collected during a trip to Iran in mid-August 2026, from at least two and ideally four speakers. A private staging area exists outside the repository (`~/Sites/persian-private/native-recordings/`) holding the recording protocol, technique guide, editor instructions, and the speaker log; identities and consent live only there, never in this repository. The corpus was substantially expanded on the explicit assumption that **there is no second trip** — the recording script now works in priority tiers (essential / high value / bank) so a speaker who gives fifteen minutes still leaves the pilot safe, and four categories were added that cannot be reconstructed afterwards: sustained held vowels for a per-speaker vowel-space reference, controlled consonant frames, an expanded minimal-pair set organised by contrast, and a homograph set (one spelling, two or three pronunciations). Room tone is now recorded at both ends of every session. Two new operational documents separate what the operator does from what the speaker reads.
- Checks: n/a — documentation only. No code, no repository changes beyond `memory/`.
- Decisions/observations: no new DECISIONS.md entry required; nothing here contradicts D9, it implements it. Two things worth knowing outside the private folder. First, **consent is now tracked as two separate permissions** — use inside the application, and inclusion of the audio file in this public repository — because a speaker may reasonably grant the first and decline the second; recordings marked *app only* never enter the repository but can still drive the engine locally. Second, the sustained-vowel recordings are what turn D9 from a design argument into a measurable claim: they allow the acoustic distance between the two reference speakers' vowel spaces to be quantified and reported rather than assumed.
- Publication: two RESEARCH_LOG entries added in the same commit — one on reference-corpus design under a single non-repeatable collection opportunity, one on speaker recruitment and recording conditions as a reportable limitation. The second lists exactly what the paper's methods section must state (recruitment, speaker characteristics, sex composition and why it is not incidental, field recording conditions, the two-tier consent procedure, items speakers rejected as unnatural) and argues the honest framing: field conditions cost precision and buy ecological validity, because learners will also be recording on their own laptops in their own rooms. Also logged an incidental finding from building the minimal-pair set — Persian's contrast density is markedly uneven, with /æ/–/ɑː/ and /o/–/uː/ generating pairs freely while clean /e/–/iː/ pairs barely exist — which argues against syllabus design that mechanically covers every vowel pair.
- Open threads: the two draft emails are still unsent (set aside deliberately for now). The September developer is still unconfirmed — noting the view formed this session that Part F should be treated as an accelerator to hire for, not a dependency to wait on, and that the decision is better taken at the end of Part E when the engine's behaviour is known. Repo Topics still not set on GitHub.
- Next step: unchanged — **BUILD_PLAN Step 14** (Part C).

## 2026-08-07 — MacBook "neo" (Claude Code) — second-machine readiness; memory folder brought current

- Done: three pieces of housekeeping, all documentation. (1) An early copy of the recording script had been committed to the repository root by accident and was removed — it contained no speaker data, only word lists and protocol, so no history rewrite was needed, but it was a stale duplicate of the expanded version held privately, which is exactly the two-copies-of-one-document hazard this project has been avoiding elsewhere. (2) `docs/START_PROMPT.md` gained a **fresh-machine variant**: the previous prompt assumed the repository was already cloned, which a new machine cannot satisfy, since it cannot read this journal to find out where work stopped until it has the repository. Version B now carries its own bootstrap — verify SSH, clone into `~/Sites/persian`, then read the documents and check Part A's tooling against the versions recorded in `SETUP_LOG.md`. It also records what a clone does *not* bring: the virtual environment (never copy one between machines — it hard-codes its own absolute path), `node_modules`, `.env`, the database, the Docker images. (3) `memory/SETUP_LOG.md` gained its **Part B section** — git identity, SSH keys and why the greeting rather than the exit code is the pass, why this repository is a clone of `langcen_base` and what the two remotes are for, the repository layout, why the folder rename had to precede Part C, and the conventions adopted along the way.
- Checks: n/a — documentation only. Working tree clean, everything pushed.
- Decisions/observations: the machine is now referred to consistently as **MacBook "neo"** in the setup log, since a second machine is expected. Three conventions are now written down rather than merely practised: no `Co-Authored-By` trailers, `.claude/` and `emails/` git-ignored, and `git status` run as its own command before any commit — the last of these arising directly from the stray file above.
- Open threads: unchanged — the two draft emails unsent; the September developer unconfirmed (treat as an accelerator to hire for, not a dependency to wait on, and decide at the end of Part E); GitHub repository Topics still not set.
- Next step: still **BUILD_PLAN Step 14** (Part C) — venv, `pip install -r requirements.txt -r requirements-dev.txt`, `npm install`, `cp .env.example .env` with `EMAIL_FILE_PATH=/Users/ped/Sites/persian/tmp_emails`, `python check_env.py`, `python src/manage.py migrate`, `npm run dev`.

## 2026-08-08 — MacBook "abacus" (Claude Code) — second machine set up; uv adopted (D10, D11)

- **Done — the second machine exists.** Pedram cloned the repository by hand into `~/Sites/persian-pronunciation`; it was renamed to `~/Sites/persian` at the start of the session, deliberately *before* any virtual environment was created (SETUP_LOG's standing rule — a venv records its own absolute path). Part A re-verified on this machine: git 2.55.0, Xcode CLT, Homebrew 6.0.15, Python 3.13.3, node 22.19.0 / npm 10.9.3, ffmpeg + ffprobe 8.1.2 all already present; **installed** pre-commit 4.6.1 and Praat 7.0; **upgraded** Docker Desktop 20.10.23 → 29.6.2 (Compose v2.15.1 → v5.3.1). Part B Steps 9–10 done: the existing `id_ed25519` key was reused and GitHub accepted it; git author name aligned from `pedbad` to `Pedram Badakhchani` so both machines sign identically; `origin` switched from HTTPS to SSH; the `scaffold` remote added — it was absent because this clone came straight from `origin`, and remotes do not travel with a clone.
- **Checks:** all ✅ passed, two after a fix (below). `ssh -T git@github.com` → "Hi pedbad!"; `git fetch` over SSH ok for both remotes; `docker run --rm hello-world` ok.
- **Failures and fixes — both worth knowing on a third machine.** (1) `open -a Praat` returned "Unable to find application named 'Praat'" immediately after install; the binary and app were both fine, macOS simply had not yet indexed the new app. Launching by full path (`open /Applications/Praat.app`) worked. (2) **Docker Desktop crashed on every launch after the upgrade** — two Dock bounces, then nothing. Its own log named the cause exactly: `loading/formatting settings-store.json: overriding "DisableHardwareAcceleration": recovered from panic`. The 2023 install's settings files were unreadable to the new backend. Fixed by moving **both** `settings-store.json` and `settings.json` aside as `.bak` in `~/Library/Group Containers/group.com.docker/`, after which Docker regenerated clean defaults. Note for future sessions: macOS privacy protection (TCC) blocks the assistant's shell from `~/Library/Group Containers`, so Pedram had to run that himself; an `osascript -e 'quit app "Docker"'` in the first attempt hung, because the app being quit was already dead — use `pkill` instead.
- **Decisions: D10 and D11 written and committed (`5e332ce`).** **D10 — uv is the Python toolchain:** `pyproject.toml` (`[project]` + `[dependency-groups] dev`) plus a committed `uv.lock` become the source of truth; the environment is `.venv`; `requirements.txt` and `requirements-dev.txt` are deleted *after* the scaffold's test suite passes; the interpreter is pinned by `.python-version` (3.13.3) and provisioned by uv; `package.json`'s dev script becomes `uv run python src/manage.py runserver`; machine setup becomes `uv sync`. **D11 — dependency version policy:** stay on the **Django 5.2 LTS line** through the pilot (`>=5.2,<5.3`, currently 5.2.17), move LTS→LTS to 6.2 after the pilot rather than to 6.1, and bump third-party packages one at a time, each gated on the test suite. Rationale for both is in DECISIONS.md; the short version is that pip had no lockfile and therefore D1's "rebuild at any point in history" promise was not actually deliverable, and that Django 6.1 was three days old on the day of the decision.
- **Considered and rejected: bun in place of npm.** The uv argument does not transfer — `package-lock.json` already pins everything with integrity hashes, so there is no reproducibility hole to close; Node's only job here is compiling the Tailwind stylesheet; and Tailwind v4's native binaries plus `pwa-asset-generator`'s browser toolchain are exactly the package shapes where alternative installers misbehave. Revisit only if CI install time becomes a real cost.
- **Observations.** (a) `CLAUDE.md`'s "Skills already installed" list does **not** match what is actually available in a Claude Code session — the `engineering:*` and `design:*` names do not exist; equivalents exist under different names (`superpowers:systematic-debugging`, `ecc:code-review`, `ecc:accessibility`, and on-stack skills the doc never mentions: `django`, `django-cotton`, `celery`, `docker`, `pytest-django`, `tailwind`). Needs correcting in the doc rewrite. (b) Node versions differ between machines (abacus 22.19.0, neo 24.19.0) — recorded rather than pinned, since the lockfile pins the Tailwind CLI itself and CSS output does not vary by Node minor. (c) The repository's `pyproject.toml` already existed for Black/Ruff config with no `[project]` section, and `.gitignore` already ignores both `.venv/` and `venv/` — so the uv migration touches less than expected. (d) No scaffold script hard-codes `venv` or `requirements` — the only such references are in markdown.
- **For MacBook "neo" — read this before working there.** Nothing needs undoing: neo never ran Part C, so there is no virtual environment or installed package on it to migrate. On neo, `git pull`, then follow the **rewritten** Step 14 (uv, `.venv`, `uv sync`) rather than the version in your memory. Two things on neo will still be wrong until fixed there: its git author name is already `Pedram Badakhchani`, so nothing to do there, but check `git remote -v` shows `origin` on **SSH** (`git@github.com:…`) rather than HTTPS, and confirm the `scaffold` remote exists. Also install uv on neo (`brew install uv`) before Step 14 — it is now a Part A tool. Its Python is 3.13.15 while `.python-version` says 3.13.3; under uv this stops mattering, because uv provisions the pinned interpreter itself.
- **Open threads (carried forward, unchanged):** the two draft emails are still unsent; the September developer is unconfirmed (decide at the end of Part E); the second native speaker is still unidentified; GitHub repository Topics still not set. The `Dockerfile.engine` pinning concern raised on 7 Aug is now formally covered by D10.
- **Next step: BUILD_PLAN Step 14 (Part C), in its D10 form** — `brew install uv`; add `[project]` + `[dependency-groups] dev` to `pyproject.toml` with **no version changes**; `uv sync`; then **`uv run pytest -q` (the scaffold's 17 tests) as the acceptance test for the migration**; only if green, delete `requirements.txt` and `requirements-dev.txt`; change `package.json`'s dev script to `uv run …`; then `cp .env.example .env` with `EMAIL_FILE_PATH=/Users/ped/Sites/persian/tmp_emails`, `uv run python check_env.py`, `uv run python src/manage.py migrate`, `npm install`, `npm run dev`, and check http://127.0.0.1:8000. **Doc rewrites still owed in the same working session:** BUILD_PLAN (a new Part A step for uv plus its checklist entry; Step 14 rewritten; Steps 21 and 35 adjusted), README (D1 build instructions), CLAUDE.md (uv, and the corrected skills list).
